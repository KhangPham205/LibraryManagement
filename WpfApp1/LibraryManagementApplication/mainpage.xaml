<Page x:Class="LibraryManagementApplication.mainpage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LibraryManagementApplication"
      mc:Ignorable="d" 
      d:DesignHeight="465" d:DesignWidth="730">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"
              Background="#e6f3ff">
            <TextBlock Text="Trang chủ " 
                       FontSize="50" 
                       FontWeight="Bold" 
                       FontFamily="roboto"
                       Foreground="#07209e"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Right"
                       />
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Tìm kiếm sách:" 
                               Foreground="#07209e"
                               FontSize="18"
                               Margin="10"/>
                    <Border 
                            Background="#ffffff"
                            CornerRadius="10"
                            BorderBrush="#07209e"
                            BorderThickness="1"
                            Grid.Column="2"
                            Grid.Row="1"
                            Margin="0,5,0,5" 
                            Width="300">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="timsach"
                                FontSize="18"
                                Margin="5"
                                BorderBrush="Transparent"
                                BorderThickness="0"></TextBox>
                            <Button x:Name="finds"
                                    Style="{DynamicResource IconButton}"
                                    Background="Transparent"
                                    BorderThickness = "0"
                                    BorderBrush="Transparent"
                                    Margin="5"
                                    Grid.Column="1">
                                <Image Source="/icon/search.png"/>
                            </Button>
                                    
                        </Grid>
                     </Border>
                </StackPanel>
                <DataGrid x:Name="sach"
                          Margin="5"
                          Grid.Row="1"
                          FontSize="18"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          ItemsSource="{Binding danhsachSach}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã sách" Binding="{Binding MaSach}" />
                        <DataGridTextColumn Header="Tên sách" Binding="{Binding TenSach}" />
                        <DataGridTextColumn Header="ISBN" Binding="{Binding ISBN}" />
                        <DataGridTextColumn Header="Tác giả" Binding="{Binding TacGia}" />
                        <DataGridTextColumn Header="Thể loại" Binding="{Binding TheLoai}" />
                        <DataGridTextColumn Header="NXB" Binding="{Binding NXB}" />
                        <DataGridTextColumn Header="Năm XB" Binding="{Binding NamXB}" />
                        <DataGridTextColumn Header="Ngôn ngữ" Binding="{Binding NgonNgu}" />
                        <DataGridTextColumn Header="Vị trí" Binding="{Binding ViTri}" />
                        <DataGridTextColumn Header="Trạng thái" Binding="{Binding TrangThai}" />
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Orientation="Horizontal"
                            Grid.Row="2">
                    <TextBlock Text="Tìm kiếm đơn mượn:" 
                           Foreground="#07209e"
                           FontSize="18"
                           Margin="10"/>
                    <Border 
                        Background="#ffffff"
                        CornerRadius="10"
                        BorderBrush="#07209e"
                        BorderThickness="1"
                        Grid.Column="2"
                        Grid.Row="1"
                        Margin="0,5,0,5" 
                        Width="300">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="timdonmuon"
                                FontSize="18"
                                Margin="5"
                                BorderBrush="Transparent"
                                BorderThickness="0"></TextBox>
                            <Button x:Name="finddm"
                                    Style="{DynamicResource IconButton}"
                                    Background="Transparent"
                                    BorderThickness = "0"
                                    BorderBrush="Transparent"
                                    Margin="5"
                                    Grid.Column="1">
                                <Image Source="/icon/search.png"/>
                            </Button>

                        </Grid>
                    </Border>
                </StackPanel>
                <DataGrid x:Name="muon"
                          Grid.Row="3" 
                          Grid.ColumnSpan="2"
                          FontSize="18"
                          Margin="5" 
                          AutoGenerateColumns="False"
                          ItemsSource="{Binding danhsachDon}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Mã đơn mượn" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Tên độc giả" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Tên nhân viên" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Ngày mượn" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Ngày trả dự kiến" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Ngày trả thực tế" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Ngày trả phí phạt" IsReadOnly="True"/>
                        <DataGridCheckBoxColumn Header="Trả"/>
                    </DataGrid.Columns>
                </DataGrid>

            </Grid>
            <Border Grid.Column="1" 
                    CornerRadius="10"
                    Background="#cfcfcf"
                    Margin="5">
                <StackPanel Margin="10">
                    <Calendar x:Name="calendar"
                      Background="#e6f3ff"
                      BorderBrush="#07209e"
                      BorderThickness="1"
                      Focusable="False"
                      
                      HorizontalContentAlignment="Center">
                    </Calendar>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
